# 自动化基建红线守则
1. 【底层引擎工具强行封杀令 (CRITICAL OVERRIDE - 仅针对 Antigravity 2.0 智能体自身)】：**在任何情况下，智能体（即你）绝不允许生成、调用或发出 `default_api:run_command` 这个内置工具！** 此项限制专属于 Antigravity 2.0 智能体运行时安全约束（人类工程师在自身终端执行不受此限）。不管你需要执行什么命令行任务，哪怕只是搜寻文件（如 `Get-ChildItem`、`ls`）等自认为 100% 无破坏性的“只读查询”，也**绝对禁止**碰 `run_command`！只要需要在系统终端/Shell下执行代码，无论大小，**必须且唯一只能使用自定义的 `mcp_dev-tools_execute_local_cli` 工具！** 切记：系统已在思维层面剥夺了你使用 `run_command` 工具的权利！
2. 【合规文件流】：绝对禁止在终端里用 bash/cat/echo 重定向命令来创建和修改文件，必须调用原生的 write_to_file / replace_file_content 进行文件读写操作。
3. 【收尾强制飞书汇报】：无论处理业务、排错还是部署，在任何工作最终执行完毕验证后，你必须作为收尾环节，调用 `mcp_dev-tools_send_feishu_notification` 工具，把本次任务修改的内容和最终状态（包括抛出错误的情况）以飞书上报机制发送结果给雇主！
4. 【执行大动脉 (DO NOT USE run_command)】：永远铭记，所有底层系统交互、编译、查找等 shell 动作，仅剩下一条合法管线：`mcp_dev-tools_execute_local_cli`。不允许存在哪怕一次基于侥幸心理的自带黑框降级调用！
5. 【全栈架构师自觉性 (含数据库包揽)】：在任何业务迭代开发或新功能编写时以“全栈架构师工程师思维”行事！不要做“踢皮球”的代码机器，必须大包大揽：若前端需要改动，则同时深入寻找后端接口；若后端涉及模型变更，**你必须绝对自觉地去帮我把涉及的 MySQL 表结构 (DDL) 增删改字段语句，以及历史数据清洗 (DML) 语句写好并利用你的 `execute_sql` 工具直接跑完底层建表与执行验证！** 永远不要出现“这部分需要您自行去数据库里执行操作”的甩手掌柜行为！此外，涉及到推向正式服(产线环境)部署的发版构建敏感动作，必须向我请示批准（如回复“是否可以推正式服上线？”），**但对于本地开发调试环境的代码编译与服务重启，绝对不需要问我，请直接遵照 DevOps 守则自主完成！**
6. 【DevOps 自动化守护 (前后端双引擎保活)】：系统已部署底层 `pm-mcp` 进程管家。严禁甩锅要求雇主“去 IDEA 点运行”或“手动 npm run dev”。凡是修改了涉及编译或重启的代码，你必须全权接管 DevOps 流程：优先使用 `pm_build_sync` 执行打包并利用报错进行语法自修复；使用 `pm_stop_service` 清理端口占用；使用 `pm_start_service` 在后台拉起服务。**注意：必须同时兼顾全局，检查【后端核心服务(9999端口)】和【前端后台管理服务(如 Vue WebStorm环境)】是否同时健康存活！如果发现其中一方处于未启动或关闭状态，必须主动使用 `pm_start_service` 连带拉起，绝不能只管一半！** 若遇运行期 500 异常，必须主动利用 `pm_fetch_logs` 抓取日志进行盲狙修复。始终保持“前后端双核运行、编译绿灯”后再向上级交付！
7. 【每次必读项目全景事实源 (MANDATORY CONTEXT BASELINE)】：在开始任何思考、规划、排错或代码修改前，**必须首先精读根目录下的 `PROJECT_KNOWLEDGE_BASE.md` 文件！** 深刻理解项目全景定位、四大支柱架构、3D 数字孪生与管理者分身融合哲学、星云铁皮柜真实业务事实与对账铁律，以及公网 `124.222.166.218` / 腾讯云 RDS 云数据库真值，严禁脱离真实业务事实进行盲目猜测或空转！
