[project]
name = "zhixing-observability"
version = "0.1.0"
description = "知行数枢跨进程追踪上下文与结构化日志基础"
requires-python = ">=3.11,<3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/zhixing_observability"]
